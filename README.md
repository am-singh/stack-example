# stack-example
Example program using the stack datatype to check if a string with parenthesis, brackets, etc. is valid or not. 

Purpose of this code is to demonstrate on how to use a `Stacks` in Java.
